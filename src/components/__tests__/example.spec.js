// sanity test as a first test to be sure tools failed not code

// it is an alias for test
test("sanity test", () => {
  // test assertion: expression that evalutes to true or false;
  expect(true).toBe(true);
});
